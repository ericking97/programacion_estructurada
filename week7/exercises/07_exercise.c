/**
 * Implemente funciones con los prototipos:
 *
 * double add(double a, double b);      // Retorna a + b.
 * double subtract(double a, double b); // Regresa a - b.
 * double multiply(double a, double b); // Regresa a * b.
 * double array_op(double array[], int size, double (*pfun)(double double));
 *
 * Los parámetros de la función array_op() son: el array para ser operado, el número de elementos en el array, y un
 * puntero a una función; definiendo la operación a ser aplicada entre los elementos sucesivos. La función array_op()
 * debe ser implementada tal que cuando la función substract() es pasada como 3er argumento, la función combina los
 * elementos alternando con los signos. Luego por ejemplo, para un array con cuatro elementos, sean {x1, x2, x3, x4},
 * calcula el valor de: x2-x2+x3-x4.
 *
 * Demuestre la opreación de estas funciones con un programa tal que sean invocadas dentro de main().
 * */
double add(double a, double b)
{
}

double subtract(double a, double b)
{
}

double multiply(double a, double b)
{
}

double array_op(double array[], int size, double (*pfun)(double double))
{
}

int main()
{
  return 0;
}